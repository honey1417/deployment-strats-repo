# simple-java-ci-cd-app/Dockerfile

# Use a lightweight official OpenJDK runtime image
FROM openjdk:17-jdk-slim

# Set the working directory in the container
WORKDIR /app

COPY target/demo-app-2.0.0-SNAPSHOT.jar app.jar

# Expose the port that the Spring Boot application listens on
EXPOSE 9000

# Define the command to run the application, explicitly setting the port
# This hardcodes the port into the image. Use -e SERVER_PORT for more flexibility.
ENTRYPOINT ["java", "-jar", "app.jar", "--server.port=9000"]